---
argument-hint: <issue-number>
---

<!-- commands/ 는 단일 파일 커맨드입니다. `/example 123` 처럼 호출하면
     아래 !`...` 줄이 셸에서 먼저 실행되어 출력이 프롬프트에 주입됩니다.
     $ARGUMENTS 는 커맨드 뒤에 입력한 값으로 치환됩니다.

     참고: 새 작업에는 commands 대신 skills/ 사용을 권장합니다.
     (skills 는 같은 /이름 호출 + 지원 파일 번들이 가능)
     이 파일은 레거시 호환 예시로만 둡니다. 필요 없으면 commands/ 폴더를 지우세요. -->

!`gh issue view $ARGUMENTS`

위 이슈를 조사하고 수정하세요.

1. 버그를 근본 원인까지 추적한다
2. 수정을 구현한다
3. 테스트를 추가/갱신한다
4. 무엇을 왜 바꿨는지 요약한다
